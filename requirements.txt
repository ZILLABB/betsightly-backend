# BetSightly — production dependencies
# Render Starter plan (2 GB RAM)

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# HTTP clients
aiohttp==3.9.1
httpx==0.25.2
requests==2.31.0

# ML / Data — keep in sync with the training environment, or joblib
# artifacts (NN pipelines, isotonic calibrators) fail to unpickle on
# deploy. Models are trained on numpy 2.x / scikit-learn 1.8.
numpy>=2.0,<3.0
pandas>=2.2
scikit-learn==1.8.0
joblib>=1.4
xgboost>=2.1
lightgbm>=4.5.0
catboost>=1.2.7
scipy>=1.13

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
python-telegram-bot[job-queue]==20.7
pywebpush==2.0.1

# Monitoring (optional)
sentry-sdk[fastapi]==1.39.1
